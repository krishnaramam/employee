CREATE TABLE test_db_migration
   (	WEB_COUNTRY VARCHAR(3) NOT NULL, 
	LOCATION BIGINT NOT NULL, 
	LOCTYPE VARCHAR(1) NOT NULL, 
	DISPATCH VARCHAR(1) DEFAULT 'N' NOT NULL, 
	PICKUP VARCHAR(1) DEFAULT 'N' NOT NULL, 
	AVAIL_SEARCH VARCHAR(1) DEFAULT 'N' NOT NULL, 
	PRIORITY SMALLINT DEFAULT 99 NOT NULL, 
	WH_BREAK_PACK_IND VARCHAR(1), 
	CAN_HOLD_GIFT_REG VARCHAR(1) DEFAULT 'N' NOT NULL, 
	REQUIRE_PAYMENT_CAPTURE VARCHAR(1) DEFAULT 'N' NOT NULL, 
	FREIGHT_RULE_TYPE VARCHAR(256) NOT NULL, 
	TRANSFER_TYPE VARCHAR(1) DEFAULT 'E' NOT NULL, 
	PERSONALISE VARCHAR(1) DEFAULT 'N' NOT NULL, 
	DISPATCH_POINT_NAME VARCHAR(250) NOT NULL);

